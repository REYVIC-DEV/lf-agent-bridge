# CONTEXT — active run

Overwritten by `src/harness/harness.ts` at the start of every run. Treat it as the
scratchpad for the run in flight, not as history — history lives in `MEMORY.md`.

## Run

| | |
|---|---|
| Run ID | `real-dry-001` |
| Started | `2026-08-26 08:22:21Z` |
| Figma URL | https://www.figma.com/design/SQojKD0axInUMkypByxBmG/HLTH?node-id=235-958 |
| File key | `SQojKD0axInUMkypByxBmG` |
| Node ID | `235:958` |
| Component | `HlthLanding` |
| Project | `/Users/randellbenedictcalilung/hlth-site` |
| Target path | `components/figma/HlthLanding.tsx` |

## Phase

| | |
|---|---|
| Current | `INIT` |
| Status | `built, not served` |
| Local URL | `—` |

## Design spec

```
runs/real-dry-001/design.json
```

## Errors

Every failed command lands here with its full output — the harness appends, it does not
overwrite, so a run that failed three times shows all three. This is what gets read
before the next attempt.

## Notes

```
—
```
