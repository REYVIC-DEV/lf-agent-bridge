{
  "name": "webforge",
  "version": "0.1.0",
  "private": true,
  "description": "Figma to Vercel automation harness. Reuses the lf-agent-bridge QA judges.",
  "scripts": {
    "harness": "ts-node src/harness/harness.ts",
    "typecheck": "tsc --noEmit",
    "build": "next build",
    "dev": "next dev",
    "start": "next start",
    "deploy:preview": "vercel --yes",
    "deploy:prod": "vercel --prod --yes"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  }
}
